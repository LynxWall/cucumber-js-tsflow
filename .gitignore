node_modules
dist
lib
.idea/
tsconfig.tsbuildinfo
cucumber-tsflow-specs/reports/*

/.yarn/*
!/.yarn/patches
!/.yarn/plugins
!/.yarn/releases
!/.yarn/sdks

# Swap the comments on the following lines if you wish to use zero-installs
# Documentation here: https://yarnpkg.com/features/zero-installs
#!/.yarn/cache
/.pnp.*
.DS_Store

# CPU profiles captured for the performance work (read with research/scripts/attribute-cpuprofile.js)
research/profiles/

# Broken support files the syntax-error spec writes for one run and removes afterwards
cucumber-tsflow-specs/*/src/fixtures/failing/generated/
